<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuang.geoslopefix.mapper.DataSlopeGeologyMapper">
    <!-- 更新斜坡类型 -->
    <update id="updateSlopeType">
        UPDATE data_slope_geology
        SET slope_type = #{slopeType}
        WHERE id = #{id}
    </update>
    <!-- 查询全部斜坡数据 -->

    <select id="selectAllAbnormalSlopeType" resultType="DataSlopeGeology">
        SELECT id,slope_type
        FROM data_slope_geology
        WHERE slope_type IS NOT NULL AND slope_type != ''
    </select>
</mapper>